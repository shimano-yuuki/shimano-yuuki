# 来訪者が Issue を立てると BOT が Connect Four を打ち返し、盤面と README を更新して Issue を閉じる。
#
# Issueタイトルは誰でも自由に書ける文字列なので、run: に ${{ ... }} で直接埋めず
# 必ず env 経由で渡すこと（埋めるとコマンドインジェクションになる）。

name: connect four

on:
  issues:
    types: [opened]

# 同時に複数のIssueが立っても盤面と push が壊れないよう直列化する
concurrency:
  group: connect4
  cancel-in-progress: false

jobs:
  play:
    if: startsWith(github.event.issue.title, 'c4:')
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: play
        env:
          ISSUE_TITLE: ${{ github.event.issue.title }}
          ISSUE_USER: ${{ github.event.issue.user.login }}
          REPO: ${{ github.repository }}
        run: node scripts/connect4.mjs

      - name: commit
        env:
          ISSUE_USER: ${{ github.event.issue.user.login }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A README.md data assets
          if git diff --staged --quiet; then
            echo "no change"
          else
            git commit -m "chore: connect four vs @$ISSUE_USER"
            git pull --rebase --autostash origin main
            git push
          fi

      - name: reply and close
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ISSUE: ${{ github.event.issue.number }}
        run: |
          gh issue comment "$ISSUE" --body-file data/last-comment.md
          gh issue close "$ISSUE"
